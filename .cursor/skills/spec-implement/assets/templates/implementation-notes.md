# 実装中ノート — {機能名}

> 実装セッション中に随時追記する「実際にやったこと」の記録。
> 実装後 理解度クイズ（understanding-quiz-impl.html）の出題材料になる。
> hearing-notes（事前の意図）と対にすることで、「想定 vs 実際」のズレを出題できる。

## 概要

{この実装セッションで何を作ったかの2〜3文サマリー}

## Deviations（計画からの逸脱）

計画（implementation-plan）どおりに進まなかった点と、その理由を記録する。
クイズの必須出題対象。逸脱がなければ「なし」と明記する。

| # | 逸脱した点 | 計画では | 実際は | 理由 |
|---|-----------|---------|--------|------|
| D-1 | {何を変えたか} | {計画上の想定} | {実際にやったこと} | {なぜ変えたか} |

## 既存コードパスへの依存で生じた挙動

diff の表面には出ないが、既存コードパスに依存して発生する挙動を記録する。
「diff を読むだけでは見えない挙動」＝クイズの中心的な出題材料。

- {既存関数/フック/設定に依存して生じた挙動と、その根拠になる既存コードパス（file:line）}

## 想定 vs 実際

hearing-notes / implementation-plan で想定した設計と、実装結果が食い違った点。

- **想定**: {事前の想定}
  **実際**: {実装結果}
  **ズレの理由**: {なぜ食い違ったか}

## 実装判断メモ

実装中に下した細かい判断（命名・分割・エラー処理の方針など）で、
後から「なぜこうしたか」を問われうるもの。

- {判断とその理由}
