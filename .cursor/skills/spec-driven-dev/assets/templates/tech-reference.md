# {機能名} · 実装の手引き

> このページは implementation-plan を読んで実装に取りかかったときに
> 「ここがよく分からない」となりがちな箇所を、
> ライブラリドキュメントの体裁でまとめたものです。
> 順番に読む必要はなく、詰まったセクションだけ拾い読みできます。

---

## 概要

{この機能が何をするか、なぜ必要か、全体像を2-3段落の散文で説明する。
 MDNの冒頭説明のように、初学者が読んで「何の話をしているか」把握できるレベルで書く。}

```
{この機能の全体的なデータフロー / アーキテクチャをASCII図で描画する}
```

{図の説明キャプション}

---

<!--
  以下、implementation-plan に登場する技術ごとにセクションを繰り返す。

  各セクションの書き方:
  - 見出しにカテゴリ（LANGUAGE / FRAMEWORK / LIBRARY / TOOL / CONCEPT）をキッカーとして付ける
  - 散文で「何か」「なぜ使うか」「どう動くか」を自然な文章で説明する
  - 専門用語が出たら、その場で噛み砕いて説明を添える
  - 仕組みはASCII図で図示する
  - コード例は implementation-plan の変更案に沿った実践的なもの
  - 重要な関数/API にはシグネチャとパラメータ表を付ける
  - 注意点やヒントは "> **メモ**" や "> **注意**" ブロックで囲む

  粒度の目安:
  - MDN の JavaScript リファレンスや React 公式ドキュメントのように
  - 各技術を「1つの辞書エントリ」ではなく「1つのミニ記事」として書く
  - 読者が「この技術って何？」から「このコード、何をしてるか分かった」まで到達できる深さ
-->

## `{CATEGORY}` {技術名}

{この技術が何か、なぜこの機能で使うかを散文で説明する。
 「◯◯は△△のためのライブラリです」のような定義から始め、
 「この機能では□□のために使います」と具体的な利用箇所に繋げる。}

{技術の仕組みをもう少し掘り下げる。
 内部でどう動くか、どんなメンタルモデルで捉えるとよいかを説明する。}

```
{この技術の動作モデルをASCII図で示す}
```

{図の説明。「上の図で◯◯が△△に渡され…」のように図と文を連携させる。}

```{lang}
{コード例: implementation-plan の変更案に沿った実践的なコード}
```

> **メモ**: {知っておくと便利なヒント、よくある勘違い、ベストプラクティスなど}

### {関数名 / API名}

```
{関数名}({引数名}: {型}): {戻り値型}
```

{この関数/APIが何をするか、どういう場面で使うかを説明する。}

| パラメータ | 型 | 説明 |
|-----------|-----|------|
| `{引数名}` | `{型}` | {説明} |

> **注意**: {この API を使う上での注意点、ハマりポイント}

---

## 用語集

| 用語 | 意味 |
|------|------|
| `{用語}` | {平易な定義} |
